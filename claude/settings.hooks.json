{
  "hooks": {
    "FileChanged": [
      {
        "hooks": [
          {
            "type": "command",
            "asyncRewake": true,
            "command": "file=$(jq -r '.file_path // empty'); if [[ \"$file\" == */.claude/plans/*.md ]]; then echo \"Plan file updated: $file — re-read it in full with the Read tool before responding.\"; exit 2; fi"
          }
        ]
      }
    ]
  }
}
